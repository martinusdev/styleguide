include ../breadcrumbs

mixin breadcrumbsKnihyRoka(props = {})
  -
    const { breadcrumbs = ["Knihy", "Knihy roka 2020"] } = props;

  section.section--breadcrumbs
    .wrapper-main
      .bar.no-mrg-bottom
        .bar__item.bar__item--shrinkable
          +breadcrumbs({breadcrumbs})