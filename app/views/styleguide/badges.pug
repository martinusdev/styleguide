extends templates/layout

//- Options
block prepend opts
  - var page = helpers.getPageIds('components.badges', l_sitemap);
  - var categoryID = page.category;
  - var pageID = page.page;

//- Content
block prepend content

  +main-info(['app/styles/modules/_badges.scss'], contents)

    +p-big Documentation for badges components.

  //- end: main info

//- Content
block append content

  //- TABS
  +content-section(contents[0], true)

    +p A badge provides an extra information that needs to be visually separated. Using badge is as simple as wrapping text into an element with <code class='l-code-inline'>.badge</code> class. If you need multiple badges in one line, wrap them with <code class='l-code-inline'>.badges</code> container.

    +demo
      +demo-example
        .badge.mb-small Default badge

        .badges
          .badge Mutilple
          .badge badges
          .badge in line.

        .badges
          .badge
            | closeable
            button(href='#').btn.btn--clean.btn--small.btn--equal.mb-none
              +icon('close').icon--small.icon--right
          .badge
            | badges
            button(href='#').btn.btn--clean.btn--small.btn--equal.mb-none
              +icon('close').icon--small.icon--right
          .badge
            | in line
            button(href='#').btn.btn--clean.btn--small.btn--equal.mb-none
              +icon('close').icon--small.icon--right

      +demo-code
        :code

    +content-sub-section('Badge colors')

      +p There are also some predefined colors that can be used:
      +demo
        +demo-example
          .badges
            each color in ['primary', 'green', 'orange', 'violet', 'blue', 'yellow', 'brown', 'error', 'transparent']
              .badge(class=`badge--${color}`)= color
        +demo-code
          :code

    +content-sub-section('Badge sizes')
      +p Bages comes in two different sizes, default and large.

      +demo
        +demo-example
          .badge.mb-small Default badge
          .badge.badge--large Large badge

        +demo-code
          :code

  +content-section(contents[1], true)

    +p Using Pills is as simple as wrapping text into an element with <code class='l-code-inline'>.pill</code> class.

    +demo
      +demo-example
        .pill.mb-small 4
        .pill.pill--white.mb-small 102
        .pill.pill--blue.mb-small 30
        .pill.pill--blue-alt.mb-small.mr-tiny #hashtag
        a.pill.pill--blue-light.mb-small Náučná
        .pill.pill--orange.mb-small 42
        .pill.pill--orange-light.mb-small 70
        .pill.pill--red.mb-small 50
        .pill.pill--red-light.mb-small 81
        .pill.pill--green.mb-small 50
        .pill.pill--green-light.mb-small 81
        .text-size-medium
          | .text-size-medium
          .pill.mb-small.text-size-medium 4
          .pill.pill--white.mb-small.text-size-medium 102
          .pill.pill--blue.mb-small.text-size-medium 30
          .pill.pill--blue-light.mb-small.text-size-medium 60
        .text-size-regular
          | .text-size-regular
          .pill.mb-small.text-size-regular 4
          .pill.pill--white.mb-small.text-size-regular 102
          .pill.pill--blue.mb-small.text-size-regular 30
          .pill.pill--blue-light.mb-small.text-size-regular 60
      +demo-code
        :code
          <span class="pill mb-small">4</span>
          <span class="pill pill--blue mb-small">4</span>
          <span class="pill pill--blue-alt mb-small">4</span>
          <span class="pill pill--blue-light mb-small">4</span>

  section#dark-mode

    .l-wrap-main(style='background-color: var(--ms-surface-primary); color: var(--ms-text-primary); padding: 2rem; border-radius: 8px;' data-theme='dark')
      header.l-section__header
        +h2(style='color: var(--ms-text-primary);') Dark Mode

      +p(style='color: var(--ms-text-primary);') This section demonstrates how badges and pills appear in dark mode. All badge variants automatically adapt to the dark theme using CSS custom properties.

      +content-sub-section('Badges in Dark Mode')

        +demo&attributes({['data-theme']: 'dark'})
          +demo-example
            .badge.mb-small Default badge

            .badges
              .badge Multiple
              .badge badges
              .badge in line.

            .badges.mb-small
              each color in ['primary', 'green', 'orange', 'violet', 'blue', 'yellow', 'brown', 'error', 'transparent']
                .badge(class=`badge--${color}`)= color

            .badge.mb-small Default badge
            .badge.badge--large Large badge

          +demo-code
            :code
              <div class="badge">Default badge</div>
              <div class="badge badge--primary">primary</div>
              <div class="badge badge--large">Large badge</div>

      +content-sub-section('Pills in Dark Mode')

        +demo&attributes({['data-theme']: 'dark'})
          +demo-example
            .pill.mb-small 4
            .pill.pill--white.mb-small 102
            .pill.pill--blue.mb-small 30
            .pill.pill--blue-alt.mb-small.mr-tiny #hashtag
            a.pill.pill--blue-light.mb-small Náučná
            .pill.pill--orange.mb-small 42
            .pill.pill--orange-light.mb-small 70
            .pill.pill--red.mb-small 50
            .pill.pill--red-light.mb-small 81
            .pill.pill--green.mb-small 50
            .pill.pill--green-light.mb-small 81

          +demo-code
            :code
              <span class="pill">4</span>
              <span class="pill pill--blue">30</span>
              <span class="pill pill--red">50</span>

//- end: section
