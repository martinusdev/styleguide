# Styleguide Docker Environment Configuration
# Copy to .env to customize

# Node environment
NODE_ENV=development

# Docker compose service name (usually no need to change)
COMPOSE_PROJECT_NAME=martinus-styleguide

# Optional: Customize container behavior
# CI=true (already set in docker-compose.yml for npm compatibility)
